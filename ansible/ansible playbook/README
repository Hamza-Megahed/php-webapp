** This how to start php-webserver and database server using ansible playbook

$ ansible-playbook docker.yml

** To install and start Docker engine on ubuntu 

$ ansible-playbook install-docker.yml --ask-sudo-pass

** To start php-webserver and database server on docker swarm

$ ansible-playbook docker-swarm.yml




